import AppKit
import SwiftUI

extension Color {
    static let dzennBackground = Color(red: 36 / 255, green: 36 / 255, blue: 36 / 255)
    static let dzennSidebarBackground = Color(red: 31 / 255, green: 31 / 255, blue: 31 / 255)
}

extension NSColor {
    static let dzennBackground = NSColor(
        calibratedRed: 36 / 255,
        green: 36 / 255,
        blue: 36 / 255,
        alpha: 1)
}
